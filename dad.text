   {isMegaMenuOpen && (
            
            <>
            {/* Overlay */}
            <div
              className="fixed inset-0 bg-black opacity-40 z-10 "
              onClick={() => setMegaMenuOpen(false)} // Close MegaMenu when clicking outside
            ></div>
  
            {/* MegaMenu */}
            <div className="absolute top-[50px] left-0 px-4 w-full  z-50">
              <MegaMenu />
            </div>
          </>
        )}